/target
**/*.rs.bk
*.pdb

# criticalup writes installation state here when a Ferrocene toolchain is installed
.criticalup/

# local development scratchpad / notes
.remember/

# blank-and-fill answer key (reference implementations of the blanked theory
# functions) — kept out of git so the answers never leak into history
/reference/
